name: invoice-print-tool

services:
  invoice-tool:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4173:4173"
      - "8765:8765"
    volumes:
      - paddle-models:/root/.paddlex
    restart: unless-stopped

volumes:
  paddle-models:
